# Decisions waiting on the user

One `## ` heading per open decision. Record before asking; an answer nobody
saw is a guess wearing a decision's clothes.

Nothing is currently waiting. Answered decisions move to `.procoder/adr/`,
which is where they stay readable six months out.

Recently closed:

- Catalog shape — answered B, shrink to a movement index. See ADR 0006.
- Action version pinning — answered "take the recommendation", pin by commit
  SHA with Dependabot. See ADR 0007.
